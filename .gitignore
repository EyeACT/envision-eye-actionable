# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.installed.cfg

# Virtualenvs
.venv/
venv/
env/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Data (large + not repo-tracked)
data/
*.log
*.jsonl

# Local conformer scratch
.conform_tmp/

# Survey outputs and model weights: this repo is public, code and docs only.
# (per-record creator names/ORCIDs, dataset member paths, raw Zenodo
# metadata, workbooks, ONNX exports with checkpoint hashes, checkpoints)
results/
models/
ckpt/
*.onnx
*.pt
*.pth
*.xlsx
*.jsonl.gz
survey_scratch/

# Local planning notes (may hold hostnames and paths; never commit)
.planning/
